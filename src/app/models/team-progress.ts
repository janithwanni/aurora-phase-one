export class TeamProgress {
  teamid: string;
  currentScore: number;
  currentRing: number;
  currentNode: number;
  currentTime: number;
  totalScore: number;
  timeLeft: number;
  totalDistance: number;
}
